/**
 * Layout das páginas privadas do dashboard.
 */
export default function PrivateLayout({ children }: { children: React.ReactNode }) {
  return (
    <div>
      {/* TODO: Sidebar Provider */}
      {/* TODO: Header */}
      {children}
    </div>
  )
}
